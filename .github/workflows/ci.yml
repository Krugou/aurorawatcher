name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  web-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Checkout Status
        run: echo "Code acquired! Like a solar flare hitting the atmosphere. ☄️📲"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: package-lock.json
      - name: Node Status
        run: echo "Node.js is ready. The engines are humming... 🚀⚙️"

      - name: Install Dependencies
        run: |
          npm ci
          echo "Web dependencies installed. npm didn't even break a sweat. 🏋️‍♂️📦"

      - name: Lint (Web)
        run: |
          npm run lint -w aurorawatcher-web
          echo "Linting passed! The code is cleaner than a fresh Finnish snowfall. ❄️✨"

      - name: Test (Web)
        run: |
          npm run test -w aurorawatcher-web
          echo "Tests passed! Everything is working better than my social life. 🧪✅"

      - name: Build (Web)
        run: |
          npm run build -w aurorawatcher-web
          echo "Web build finished! It's so fast, even the auroras are jealous. 💅🌌"

  bot-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Checkout Status
        run: echo "Bot source secured. Preparing for digital consciousness. 💾👾"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: package-lock.json
      - name: Node Status
        run: echo "Node installed. The bot has a brain now. 🧠⚡"

      - name: Install Dependencies
        run: |
          npm ci
          echo "Bot dependencies ready. It's time to crunch some data. 🥨📊"

      - name: Build (Bot)
        run: |
          npm run build -w aurorawatcher-bot
          echo "Bot compiled! Ready to judge some solar wind. 💨📏"
